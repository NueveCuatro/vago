<form method="post" action="{{ path('app_gallery_delete', {'id': gallery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery.id) }}">
    <button type="btn" class="btn btn-danger">delete</button>
</form>
