{% extends 'base.html.twig' %}

{% block title %}Pilote{% endblock %}

{% block body %}
    <h1>Pilote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pilote.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pilote.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pilote.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pilote_index') }}">back to list</a>

    <a href="{{ path('app_pilote_edit', {'id': pilote.id}) }}">edit</a>

    {{ include('pilote/_delete_form.html.twig') }}
{% endblock %}
